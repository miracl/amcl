dist: xenial
language: generic

env:
  matrix:
    - AMCL_LANG=cpp
    - AMCL_LANG=js
    - AMCL_LANG=rust
    - AMCL_LANG=swift
    - AMCL_LANG=wasm-c

script:
  - ./scripts/travis.sh

notifications:
  email: false
